# i3status configuration file.
# see "man i3status" for documentation.

# It is important that this file is edited as UTF-8.
# The following line should contain a sharp s:
# ß
# If the above line is not correctly displayed, fix your editor first!

general {
        colors = true
        interval = 5
	markup = pango
	color_good = "#b8bb26"
	color_bad = "#fb4934"
	color_degraded = "#fabd2f"

}

# order += "ipv6"
order += "wireless _first_"
order += "ethernet _first_"
order += "battery all"
order += "cpu_temperature 0"
order += "cpu_usage"
order += "disk /"
#order += "load"
order += "memory"
order += "volume master"
order += "tztime local"

wireless _first_ {
        format_up = "直 (%quality at %essid) %ip"
        format_down = "睊 down"
}

ethernet _first_ {
#        format_up = ": %ip (%speed)" 
        format_up = " (%speed)"
        format_down = "E: down"
}

battery all {
        format = "<span background='#79740e' >🔋 %status %percentage %remaining </span>"
	status_chr = "⚡ CHR"
        status_bat = "🔋 BAT"
        status_unk = "? UNK"
        status_full = " FULL"
#	last_full_capacity = true # -> this is show the real capacity of the battery or not. Set false to see how worn the battery is.
}

disk "/" {
	format = "💾 %avail"
#        format = "%avail"
}

cpu_temperature 0 {
	format = "🔥 %degrees °C"
}

cpu_usage {	
#	format = "🚀 %usage CPU_0: %cpu0 CPU_1: %cpu1"
	format = "🚀 %usage"
}

#load {
#        format = "%1min"
#}

memory {
        format = "🧠 %used - %available"
        threshold_degraded = "1G"
        format_degraded = "MEMORY < %available"
}

volume master {
	format = "🔊 %volume"
	format_muted = "🔇 muted %volume"
	device = "pulse"
	mixer = "Master"
	mixer_idx = 0
#	min_width = 100
}

tztime local {
        format = "⌚ %Y-%m-%d %H:%M:%S"
}
